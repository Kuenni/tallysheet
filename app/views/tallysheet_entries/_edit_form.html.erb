<%= form_for(@tallysheet_entry, html: {class: "form-horizontal"}) do |f| %>
<% if @tallysheet_entry.errors.any? %>
<div id="error_explanation">
	<h2><%= @tallysheet_entry.errors.count %> Fehler verhinderten das Speichern dieses Strichlisteneintrags:</h2>

	<ul>
		<% @tallysheet_entry.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="form-group">
	<%= f.label :created_at, "Erstellt am", class: "col-sm-2 form-label" %>
	<div class="col-sm-10">
		<%= f.text_field :created_at, class: "form-control", required: "required" %>
	</div>
</div>

<div class="form-group">
	<%= f.label :consumer, "Name", class: "col-sm-2 form-label" %>
	<div class="col-sm-10">
		<%= f.collection_select :consumer_id, @consumers, :id, :name, {include_blank: true}, class: "form-control", required: "required" %>
	</div>
</div>

<div class="form-group">
	<%= f.label :beverage, "Getränk", class: "col-sm-2 form-label" %>
	<div class="col-sm-10">
		<%= f.collection_select :beverage_id, @beverages, :id, :name, {include_blank: true}, class: "form-control", required: "required" %>
	</div>
</div>

<div class="form-group">
	<%= f.label :amount, "Anzahl", class: "col-sm-2 form-label" %>
	<div class="col-sm-10">
		<%= f.number_field :amount, class: "form-control", required: "required", min: "1", max: "10000", step: "1" %>
	</div>
</div>

<div class="form-group">
	<div class="offset-sm-1 col-sm-10">
		<%= f.submit "Speichern", class: "btn btn-primary btn-sm" %>
	</div>
</div>

<% end %>
