<div class="page-header">
	<h1><%= @consumer.name %> <small>Zahlungen</small></h1>
</div>

<p class="text-muted"><%= page_entries_info @payments %></p>

<table class="table table-striped table-hover">
	<thead>
		<th>Datum</th>
		<th>Betrag</th>
	</thead>
	<% @payments.each do |e| %>
	<tr>
		<td><%= e.created_at.to_formatted_s(:long) %></td>
		<td><%= "%.2f" % e.amount %> &euro;</td>
	</tr>
	<% end %>
</table>

<%= will_paginate @payments, :renderer => BootstrapPagination::Rails %>

<hr>

<%= link_to 'Zurück', consumer_path(@consumer), class: "btn btn-secondary btn-sm" %>